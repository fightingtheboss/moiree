<% flash.each do |flash_type, message| %>
  <div data-controller="flash" data-action="animationend->flash#remove" class="text-sm rounded-full text-white <%= flash_type == "alert" ? "bg-red-700/85" : "bg-indigo-900/60" %> px-4 py-2 animate-appear-then-fade">
    <%= message %>
  </div>
<% end %>
