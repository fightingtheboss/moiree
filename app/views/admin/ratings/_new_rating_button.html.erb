<%= link_to(
  "Rate",
  new_admin_festival_edition_selection_rating_path(festival, edition, selection),
  data: {
    turbo_frame: "modal",
    controller: "modal",
    action: "modal#show",
    "modal-dialog-param": "global-modal",
    "modal-target-param": dom_id(edition, :films)
  },
  class: "inline-block bg-indigo-500 hover:bg-indigo-700 text-white font-semibold py-1 px-2 rounded text-xs",
  id: dom_id(selection, :rate_button_for)
) %>
