<%= turbo_frame_tag "modal" do %>
  <h1 class="text-2xl font-semibold mb-4">Rate <span class="text-indigo-500"><%= @selection.film.title %></span></h1>
  <%= render partial: "form", locals: { festival: @festival, edition: @edition, selection: @selection, rating: @rating } %>
<% end %>
