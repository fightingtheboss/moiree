<div class="container mx-auto my-8">
  <%= turbo_frame_tag "modal" do %>
    <h1 class="text-2xl font-semibold mb-4">Edit <%= @film.title %> for <%= @edition.code %></h1>
    <%= render(partial: "admin/selections/form", locals: { selection: @selection, film: @film, edition: @edition, tmdb_results: @tmdb_results }) %>
  <% end %>
</div>
