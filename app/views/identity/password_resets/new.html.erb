<h1 class="font-semibold text-xl md:text-2xl mb-4">🤔 Forgot your password?</h1>
<p class="mb-4">Give us your email and we'll send you a link to set a new one.</p>

<%= form_with(url: identity_password_reset_path) do |form| %>
  <div class="mb-4">
    <%= form.label :email, class: "block text-gray-700 text-sm font-bold mb-2" %>
    <%= form.email_field :email, required: true, autofocus: true, class: "block w-full rounded-md border-0 py-1.5 text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 placeholder:text-gray-400 focus:ring-2 focus:ring-inset focus:ring-indigo-500 sm:text-sm sm:leading-6" %>
  </div>

  <div>
    <%= form.submit "Send password reset email", class: "bg-indigo-500 hover:bg-indigo-700 text-white font-semibold py-2 px-4 rounded focus:outline-none focus:shadow-outline" %>
  </div>
<% end %>
